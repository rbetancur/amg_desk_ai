# Database Configuration
# PostgreSQL connection string (Supabase o PostgreSQL local)
# Format: postgresql://user:password@host:port/database
# Required: Yes
DATABASE_URL=postgresql://user:password@localhost:5432/database

# Supabase Configuration
# Supabase project URL
# Required: Yes (para validación JWT)
SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key (para validar tokens JWT de usuarios)
# Required: Yes (para validación JWT)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase service role key (solo para operaciones del sistema/Agente AI)
# Required: No (solo necesario para Agente AI)
# WARNING: Never expose in frontend, only use for system operations
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# API Security
# Secret key para autenticación de endpoints de acción (Amerika y Dominio)
# Required: Yes
# Generate a secure random string (e.g., using: openssl rand -hex 32)
API_SECRET_KEY=your-secret-api-key-here

# CORS Configuration
# Allowed origins for CORS (comma-separated)
# Required: Yes (para frontend en Vercel)
# Format: https://app.vercel.app,http://localhost:3000
CORS_ORIGINS=https://your-app.vercel.app,http://localhost:3000

# Application Configuration
# Project name (optional, default: "AGM Desk AI Backend")
PROJECT_NAME=AGM Desk AI Backend

# Application version (optional, default: "0.1.0")
VERSION=0.1.0
