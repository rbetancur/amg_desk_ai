# ============================================================================
# AGM Desk AI Backend - Variables de Entorno
# ============================================================================
# Copia este archivo a .env y reemplaza los valores con tus credenciales reales
# IMPORTANTE: Nunca commitees el archivo .env al repositorio

# ============================================================================
# Base de Datos (Supabase - REQUERIDO)
# ============================================================================
# Connection String de Supabase
# Obtener desde: Supabase Dashboard > Settings > Database > Connection String
# Usar Connection Pooling (Transaction mode) para producción
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres

# ============================================================================
# Supabase Configuration (REQUERIDO)
# ============================================================================
# Project URL
# Obtener desde: Supabase Dashboard > Settings > API > Project URL
SUPABASE_URL=https://[PROJECT-REF].supabase.co

# Anon Public Key (para cliente frontend)
# Obtener desde: Supabase Dashboard > Settings > API > anon public key
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# JWT Secret (para validar tokens JWT)
# Obtener desde: Supabase Dashboard > Settings > API > JWT Secret
SUPABASE_JWT_SECRET=your-jwt-secret-here

# Service Role Key (Opcional - requerida para Agente AI)
# ⚠️ MANTÉN ESTA CLAVE SEGURA - permite bypass de RLS
# Obtener desde: Supabase Dashboard > Settings > API > service_role key (secret)
SUPABASE_SERVICE_ROLE_KEY=

# ============================================================================
# API Authentication (REQUERIDO)
# ============================================================================
# API Key para autenticar endpoints de acción (Amerika/Dominio)
# Debe coincidir con la usada por el Agente AI
API_SECRET_KEY=dev-api-secret-key-12345

# ============================================================================
# CORS Configuration (REQUERIDO)
# ============================================================================
# Lista de orígenes permitidos separados por comas
# Para desarrollo local, incluir: http://localhost:3000,http://localhost:5173,http://localhost:8080
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# ============================================================================
# Redis Configuration (Opcional - para cache)
# ============================================================================
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# ============================================================================
# Application Configuration
# ============================================================================
PROJECT_NAME=AGM Desk AI Backend
VERSION=0.1.0
